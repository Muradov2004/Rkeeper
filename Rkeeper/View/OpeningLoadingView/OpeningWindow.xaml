<Window x:Class="Rkeeper.View.OpeningLoadingView.OpeningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rkeeper.View.OpeningLoadingView"
        mc:Ignorable="d"
        Title="OpeningWindow" Height="230" Width="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        Icon="../../Assets/IconImage/rkeeper.ico"
        WindowStartupLocation="CenterScreen">
    <Grid>

        <Grid.RowDefinitions>

            <RowDefinition/>
            <RowDefinition Height="auto"/>

        </Grid.RowDefinitions>

        <Image Source="../../Assets/IconImage/rkeeper.ico"/>

        <TextBlock Text="RKeeper" FontSize="25" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontWeight="Bold" Foreground="LightSalmon"/>

        <TextBlock Text="Preparing Project" FontSize="11" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontWeight="Bold" Margin="10,0" Foreground="Gray"/>

        <ProgressBar Grid.Row="1" Margin="10" Height="25" Value="0" IsIndeterminate="True" Foreground="LightSalmon"/>

    </Grid>
</Window>
