<UserControl x:Class="Rkeeper.View.MainWindowComponentsView.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rkeeper.View.MainWindowComponentsView"
             mc:Ignorable="d" 
             Margin="20,10,0,0"
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition  Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="LightGray">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid Margin="18,15,18,0" Background="WhiteSmoke">
                <Grid.RowDefinitions>
                    <RowDefinition Height="8*"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Name="OrderGrid">
                    <ListView x:Name="lsit" Background="#d6efeb" ItemsSource="{Binding }" >
                        <ListView.View>
                            <GridView>
                                <GridView.Columns>
                                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="auto"/>
                                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Surname}" Width="auto"/>
                                    <GridViewColumn Header="Amount" DisplayMemberBinding="{Binding Surname}" Width="auto"/>
                                </GridView.Columns>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
                <Button Grid.Row="1" Margin="15,8" FontWeight="Bold" FontSize="17" Content="A d d  O r d e r" Command="{Binding AddOrderCommand}"/>
            </Grid>
            <Button Grid.Row="1" Margin="20,10" FontSize="28" Height="50" Content="B i l l" FontWeight="Bold" Command="{Binding BillCommand}"/>

        </Grid>

        <ScrollViewer Grid.Column="1">
            <WrapPanel HorizontalAlignment="Center">
                <WrapPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Width" Value="200"/>
                        <Setter Property="Height" Value="150"/>
                        <Setter Property="Margin" Value="20,10"/>
                        <Setter Property="FontSize" Value="25"/>
                    </Style>
                </WrapPanel.Resources>
                <Button Content="Tabel1" Command="{Binding TableCommand}"/>
                <Button Content="Tabel2" Command="{Binding TableCommand}"/>
                <Button Content="Tabel3" Command="{Binding TableCommand}"/>
                <Button Content="Tabel4" Command="{Binding TableCommand}"/>
                <Button Content="Tabel5" Command="{Binding TableCommand}"/>
                <Button Content="Tabel6" Command="{Binding TableCommand}"/>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
