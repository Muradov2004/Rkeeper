<UserControl x:Class="Rkeeper.View.MainWindowComponentsView.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rkeeper.View.MainWindowComponentsView"
             mc:Ignorable="d" 
             Margin="20,10,0,0"
             d:DesignHeight="400" d:DesignWidth="510">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="Margin" Value="20,10"/>
            <Setter Property="FontSize" Value="25"/>
        </Style>
    </UserControl.Resources>
    <ScrollViewer>
        <WrapPanel HorizontalAlignment="Center">
            <Button Content="Tabel1"/>
            <Button Content="Tabel2"/>
            <Button Content="Tabel3"/>
            <Button Content="Tabel4"/>
            <Button Content="Tabel5"/>
            <Button Content="Tabel6"/>
        </WrapPanel>
    </ScrollViewer>
</UserControl>
